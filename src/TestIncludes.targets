<Project>
  <ItemGroup>
    <!-- Include all framework-specific split files based on TargetFramework -->
    <Compile Include="$(MSBuildThisFileDirectory)Split\$(TargetFramework)\**\*.cs">
      <Link>Pollyfill\%(RecursiveDir)%(Filename).cs</Link>
    </Compile>
  </ItemGroup>
</Project>